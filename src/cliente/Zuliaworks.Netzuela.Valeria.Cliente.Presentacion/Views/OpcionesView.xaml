<UserControl x:Class="Zuliaworks.Netzuela.Valeria.Cliente.Presentacion.Views.OpcionesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Label>Intervalo de sincronización:</Label>
            <Slider Name="sldr_Sincronizacion" Minimum="30" Maximum="3600" IsSnapToTickEnabled="True" TickFrequency="15" Width="250" Value="{Binding Path=BarraDeDesplazamiento, Mode=TwoWay}"/>
            <TextBlock Text="{Binding Path=IntervaloCompensacionString}"/>
        </StackPanel>
    </Grid>
</UserControl>

